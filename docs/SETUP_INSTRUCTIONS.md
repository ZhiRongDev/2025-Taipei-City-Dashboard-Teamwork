# 🚨 GitHub Pages 首次設置說明

## 錯誤訊息
```
Get Pages site failed. Please verify that the repository has Pages enabled 
and configured to build using GitHub Actions
```

## ✅ 解決方案

這個錯誤是正常的！這是因為 **GitHub Pages 尚未啟用**。你需要先手動啟用 GitHub Pages，然後再推送代碼。

---

## 📋 正確的設置順序

### 步驟 1: 先啟用 GitHub Pages（必須先做！）

1. **前往你的 repository 設置頁面**:
   ```
   https://github.com/ZhiRongDev/2025-Taipei-City-Dashboard-Teamwork/settings/pages
   ```

2. **在 "Build and deployment" 區域**:
   - 找到 **Source** 下拉選單
   - 選擇 **GitHub Actions**
   - GitHub 會自動保存設置

3. **確認設置成功**:
   - 頁面上方應該顯示：
     ```
     GitHub Pages is currently disabled. Select a source below to enable GitHub Pages for this repository.
     ```
   - 選擇後會變成：
     ```
     Your site is ready to be published at https://zhirongdev.github.io/2025-Taipei-City-Dashboard-Teamwork/
     ```

---

### 步驟 2: 推送代碼到 GitHub

**重要**: 只有在完成步驟 1 之後才能推送！

```bash
# 添加所有新檔案
git add .github/workflows/deploy.yml
git add vite.config.js
git add public/.nojekyll
git add DEPLOYMENT_GUIDE.md
git add GITHUB_PAGES_SETUP.md
git add SETUP_INSTRUCTIONS.md

# 提交
git commit -m "Add GitHub Pages deployment workflow

- Add GitHub Actions workflow for automatic deployment
- Update vite.config.js with production base URL
- Add .nojekyll file for proper routing
- Add deployment documentation"

# 推送到 main 分支
git push origin main
```

---

### 步驟 3: 等待首次部署完成

1. **前往 Actions 頁面**:
   ```
   https://github.com/ZhiRongDev/2025-Taipei-City-Dashboard-Teamwork/actions
   ```

2. **查看 "Deploy to GitHub Pages" workflow**:
   - 應該會看到一個正在運行的 workflow
   - 等待綠色勾選標記 ✅
   - 首次部署通常需要 **3-5 分鐘**

3. **部署完成後**:
   - 訪問你的網站: `https://zhirongdev.github.io/2025-Taipei-City-Dashboard-Teamwork/`
   - 所有頁面和功能應該正常運作

---

## 🎯 視覺化設置流程

```
第一次設置（只需做一次）：
┌─────────────────────────────────────┐
│ 1. 前往 Settings > Pages           │
│ 2. Source 選擇 "GitHub Actions"    │
│ 3. 保存設置                         │
└─────────────────────────────────────┘
              ↓
┌─────────────────────────────────────┐
│ 4. git add 所有檔案                 │
│ 5. git commit                       │
│ 6. git push origin main             │
└─────────────────────────────────────┘
              ↓
┌─────────────────────────────────────┐
│ 7. 前往 Actions 頁面                │
│ 8. 等待部署完成 (3-5 分鐘)         │
│ 9. 訪問你的網站！                   │
└─────────────────────────────────────┘

之後的更新（自動化）：
┌─────────────────────────────────────┐
│ 1. 修改代碼                         │
│ 2. git add .                        │
│ 3. git commit -m "更新說明"         │
│ 4. git push origin main             │
│    ↓                                │
│ 5. 自動構建和部署！ ✨              │
└─────────────────────────────────────┘
```

---

## 📸 設置截圖指南

### 在 GitHub Settings > Pages 頁面你會看到：

**啟用前**:
```
┌──────────────────────────────────────────┐
│ GitHub Pages                              │
├──────────────────────────────────────────┤
│ GitHub Pages is currently disabled.       │
│ Select a source below to enable           │
│ GitHub Pages for this repository.         │
│                                           │
│ Source                                    │
│ ┌──────────────────────┐                 │
│ │ Deploy from a branch ▼│  ← 不要選這個   │
│ └──────────────────────┘                 │
│ ┌──────────────────────┐                 │
│ │ GitHub Actions        │  ← 選這個！     │
│ └──────────────────────┘                 │
└──────────────────────────────────────────┘
```

**啟用後**:
```
┌──────────────────────────────────────────┐
│ GitHub Pages                              │
├──────────────────────────────────────────┤
│ ✅ Your site is ready to be published at │
│ https://zhirongdev.github.io/            │
│ 2025-Taipei-City-Dashboard-Teamwork/     │
│                                           │
│ Source                                    │
│ ┌──────────────────────┐                 │
│ │ GitHub Actions     ✓  │                │
│ └──────────────────────┘                 │
└──────────────────────────────────────────┘
```

---

## 🐛 如果遇到問題

### 問題 1: 找不到 Settings > Pages

**解決方案**:
1. 確保你是 repository 的擁有者或有管理員權限
2. 如果是 fork 的 repository，確保 fork 設置允許 Pages
3. 檢查 repository 是否是 public（private repo 需要 GitHub Pro）

### 問題 2: 選擇了 GitHub Actions 但沒有反應

**解決方案**:
1. 重新整理頁面
2. 確認頁面上方顯示 "Your site is ready to be published"
3. 如果還是沒有，嘗試登出再登入 GitHub

### 問題 3: 推送後仍然顯示錯誤

**解決方案**:
1. 確認你已經在 Settings > Pages 中啟用了 GitHub Actions
2. 檢查 Actions 頁面是否有運行的 workflow
3. 查看 workflow 的詳細日誌找出錯誤原因

### 問題 4: Actions 運行但部署失敗

**檢查事項**:
```bash
# 1. 確認本地構建成功
npm run build

# 2. 檢查 .github/workflows/deploy.yml 是否存在
ls -la .github/workflows/deploy.yml

# 3. 檢查 vite.config.js 配置
cat vite.config.js | grep base

# 應該看到:
# base: process.env.NODE_ENV === 'production'
#   ? '/2025-Taipei-City-Dashboard-Teamwork/'
#   : '/'
```

---

## ✅ 成功檢查清單

在推送代碼之前，確保：

- [ ] 你是 repository 的擁有者或有管理員權限
- [ ] Repository 是 public（或有 GitHub Pro）
- [ ] 已前往 Settings > Pages
- [ ] Source 已選擇 "GitHub Actions"
- [ ] 頁面顯示 "Your site is ready to be published"
- [ ] 所有檔案都已添加到 git
- [ ] 本地構建測試成功 (`npm run build`)

完成這些檢查後，才執行 `git push origin main`

---

## 📞 需要更多幫助？

1. **查看 GitHub Actions 日誌**:
   - https://github.com/ZhiRongDev/2025-Taipei-City-Dashboard-Teamwork/actions
   - 點擊失敗的 workflow 查看詳細錯誤

2. **檢查 GitHub Pages 狀態**:
   - https://github.com/ZhiRongDev/2025-Taipei-City-Dashboard-Teamwork/settings/pages
   - 確認 Source 是否正確設置

3. **參考完整文檔**:
   - [DEPLOYMENT_GUIDE.md](DEPLOYMENT_GUIDE.md)
   - [GITHUB_PAGES_SETUP.md](GITHUB_PAGES_SETUP.md)

---

## 🎉 設置完成後

一旦設置成功，未來的部署就非常簡單：

```bash
# 日常工作流程
git add .
git commit -m "Update feature"
git push origin main

# 就這樣！GitHub Actions 會自動部署
# 2-3 分鐘後訪問你的網站查看更新
```

你的網站將部署在:
```
https://zhirongdev.github.io/2025-Taipei-City-Dashboard-Teamwork/
```

---

**重要提醒**: 
- 首次設置**必須**先在 GitHub Settings 中啟用 Pages
- 啟用後才能推送代碼
- 之後的更新就完全自動化了！

**設置順序**: Settings > Pages → 選擇 GitHub Actions → git push → 等待部署 → 完成！

---

**文檔創建日期**: 2026-01-03  
**創建者**: Claude Sonnet 4.5
